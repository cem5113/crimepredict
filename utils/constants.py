from __future__ import annotations

KEY_COL = "GEOID"        # tüm merge ve groupby anahtarı
RISK_COL = "risk_score"  # kayan noktalı risk skor kolon adı (float)
TIME_COL = "timestamp"   # varsa tarih/saat (UTC/yerel fark etmiyor)
DATE_COL = "date"        # günlük özet için kullanılabilir
